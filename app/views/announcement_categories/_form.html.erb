 <table>
  <tr>
    <th> <%= field.label(:name, "Nazwa kategorii: ")%> </th>
    <td> <%= field.text_field(:name)%></td>
  </tr>
  <tr>
    <th> <%= field.label(:image, "Obaraz kategorii: ")%> </th>
    <td> <%= field.file_field(:image)%></td>
  </tr>

  <% if !@category.image%>
    <tr>
      <th> Zdjęcie </th>
      <td> <%= image_tag @category.image %></td>
    </tr>
  <% end %>
</table>
