
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href=/>Wypożyczalnia Sprzętu Elektronicznego</a>
    </div>
    <div>
      <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav navbar-right">
          <li class="<% yield(:home)%>"><%= link_to("Home", {:controller => 'welcome', :action => 'index'}) %></li>
          <li class="dropdown"><a class="dropdown-toggle" data-toggle="dropdown" href="#">Kategorie<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <% AnnouncementCategory.all.each do |category| %>
                <li class="<% yield(:categories)%>"><%= link_to("#{category.name}", {:controller => 'announcement', :action => 'view_by_category', :id => category.id})%></li>
              <% end %>
            </ul>
          </li>
          <li class="<% yield(:login)%>"><%= link_to("Zaloguj", {:controller => 'welcome', :action => 'login'})%></li>
          <li class="<% yield(:registration)%>"><%= link_to("Zarejestruj", {:controller => 'welcome', :action => 'registration'}) %></li>
          <li class="<% yield(:add)%>"><%= link_to("Dodaj Ogłoszenie!", {:controller => 'announcement', :action => 'add'})%></li>
        </ul>
      </div>
    </div>
  </div>
</nav>
