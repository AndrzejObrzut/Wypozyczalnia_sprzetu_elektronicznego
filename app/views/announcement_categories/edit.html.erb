<div class="container">
  <h1>Edycja kategorii</h1>

  <%= link_to("Wróć", {:action => 'index'}) %>

  <%= form_for(:category, :url => {:action => "update", :id => @category.id}) do |field|%>

    <table>
      <tr>
        <th> <%= field.label(:name, "Nazwa kategorii: ")%> </th>
        <td> <%= field.text_field(:name)%></td>
      </tr>

    </table><br>
    <%= submit_tag("Aktualizuj") %>
  <% end %>
</div>
